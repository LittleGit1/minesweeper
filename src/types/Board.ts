export type BoardSettings = {
    columns: number; // The number of columns in board the grid (x axis)
    rows: number; // The number of rows in the board grid (y axis)
    boardMines: number; // The number of mines to be generated 
}